bash <(cat <<EOF
#!/bin/bash
# ==============================================================================
#   💎 TikTok 直播节点：修正版交付脚本 (去除风险项，保留核心优化)
#   保留：DNS锁 (抗云厂商重置) + MSS钳制 (抗隧道分片) + QUIC阻断 (强吃BBRv3红利)
#   移除：Warm-up (无效流量) + Renice (系统风险)
# ==============================================================================

# --- UI 颜色 ---
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m'

echo -e "\${YELLOW}>>> 开始部署修正版优化策略...\${NC}"

# 1. 基础工具安装 (保留)
apt update -y >/dev/null 2>&1
DEBIAN_FRONTEND=noninteractive apt install -y nscd iptables-persistent dnsutils e2fsprogs >/dev/null 2>&1

# 2. DNS 锁定 (针对华为云必须保留)
# 先解锁以防万一
chattr -i /etc/resolv.conf >/dev/null 2>&1
# 配置 NSCD 缓存加速
sed -i 's/enable-cache\s\+hosts\s\+no/enable-cache hosts yes/' /etc/nscd.conf
systemctl enable nscd >/dev/null 2>&1
systemctl restart nscd >/dev/null 2>&1
# 写入国际通用 DNS 并锁定
echo "nameserver 1.1.1.1" > /etc/resolv.conf
echo "nameserver 8.8.8.8" >> /etc/resolv.conf
chattr +i /etc/resolv.conf
echo -e "\${GREEN}[OK] DNS 已锁定并开启缓存 (防止华为云重置)\${NC}"

# 3. 交通管制 (针对 Argo 隧道必须保留)
# 清理旧规则
iptables -t mangle -F
iptables -F

# MSS 钳制 1360 (防止 Argo 隧道分片导致直播卡顿)
iptables -t mangle -A OUTPUT -p tcp --tcp-flags SYN,RST SYN -j TCPMSS --set-mss 1360
iptables -t mangle -A FORWARD -p tcp --tcp-flags SYN,RST SYN -j TCPMSS --set-mss 1360

# 阻断 UDP 443 (强迫 TikTok 降级走 TCP，从而享受 BBRv3 加速)
iptables -A OUTPUT -p udp --dport 443 -j DROP
iptables -A FORWARD -p udp --dport 443 -j DROP

# 持久化规则
netfilter-persistent save >/dev/null 2>&1
echo -e "\${GREEN}[OK] MSS 已钳制(1360)，QUIC 已阻断(强制TCP)\${NC}"

# 4. 清理之前的“智商税”定时任务 (如果装过)
crontab -l 2>/dev/null | grep -v "warmup_pro.sh" | grep -v "boost_proxy.sh" | crontab -
rm -f /usr/local/bin/warmup_pro.sh
rm -f /usr/local/bin/boost_proxy.sh
echo -e "\${GREEN}[OK] 已清理无用的预热和提权脚本\${NC}"

echo -e "\n\${GREEN}🎉 优化完成！这是最稳健、副作用最小的方案。\${NC}"
EOF
)














bash <(cat <<EOF
#!/bin/bash
# ======================================================================
#   🏆 TikTok 直播节点：全维度终极验收审计 (Ultimate Audit vMax)
#   覆盖：BBR/内核/DNS/防火墙/协议栈/自动化/缓存/运维
# ======================================================================

# --- 颜色定义 ---
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
CYAN='\033[0;36m'
NC='\033[0m'

# --- 依赖检查 ---
if ! command -v dig &> /dev/null; then apt update -y && apt install dnsutils -y; fi
if ! command -v lsattr &> /dev/null; then apt install e2fsprogs -y; fi

clear
echo -e "\${CYAN}============================================================\${NC}"
echo -e "           📊 VPS 全栈环境深度审计报告 (TikTok专用)"
echo -e "\${CYAN}============================================================\${NC}"

# ===========================
# 1. 内核与性能层 (The Engine)
# ===========================
echo -e "\n\${YELLOW}[1. 内核与性能动力]\${NC}"

# 1.1 BBR 检测
BBR_CHECK=\$(sysctl net.ipv4.tcp_congestion_control | awk '{print \$3}')
if [[ "\$BBR_STATUS" == "bbr" ]] || [[ "\$BBR_CHECK" == *"bbr"* ]]; then
    echo -e "TCP 拥塞控制 (BBR)   : \${GREEN}✅ 已开启 (推流动力核心)\${NC}"
else
    echo -e "TCP 拥塞控制 (BBR)   : \${RED}❌ 未开启 (直播会卡顿)\${NC}"
fi

# 1.2 IPv6 禁用检测
IPV6_CHECK=\$(sysctl net.ipv6.conf.all.disable_ipv6 | awk '{print \$3}')
if [[ "\$IPV6_CHECK" == "1" ]]; then
    echo -e "IPv6 禁用状态        : \${GREEN}✅ 已彻底禁用 (防泄露/防绕路)\${NC}"
else
    echo -e "IPv6 禁用状态        : \${RED}❌ 未禁用 (存在隐患)\${NC}"
fi

# 1.3 文件打开数限制
ULIMIT_CHECK=\$(ulimit -n)
if [[ "\$ULIMIT_CHECK" -gt 60000 ]]; then
    echo -e "最大连接数限制       : \${GREEN}✅ 已解锁 (\$ULIMIT_CHECK) (支持高并发)\${NC}"
else
    echo -e "最大连接数限制       : \${RED}❌ 默认值 (\$ULIMIT_CHECK) (容易断流)\${NC}"
fi

# 1.4 网卡队列长度
IFACE=\$(ip route | grep default | awk '{print \$5}' | head -n1)
TXQ=\$(cat /sys/class/net/\$IFACE/tx_queue_len)
if [[ "\$TXQ" -ge 10000 ]]; then
    echo -e "网卡发送队列         : \${GREEN}✅ 已扩容 (\$TXQ) (防微观丢包)\${NC}"
else
    echo -e "网卡发送队列         : \${RED}❌ 未优化 (\$TXQ) (建议设为10000)\${NC}"
fi

# ===========================
# 2. DNS 与 解析层 (The Brain)
# ===========================
echo -e "\n\${YELLOW}[2. DNS 解析与安全]\${NC}"

# 2.1 DNS 锁定状态
ATTR=\$(lsattr /etc/resolv.conf)
if [[ "\$ATTR" == *"i"* ]]; then
    echo -e "配置文件防篡改锁     : \${GREEN}✅ 已焊死 (+i 生效)\${NC}"
else
    echo -e "配置文件防篡改锁     : \${RED}❌ 未锁定 (重启后可能被云厂商修改)\${NC}"
fi

# 2.2 DNS 内容检查
CONF=\$(cat /etc/resolv.conf)
if [[ "\$CONF" == *"1.1.1.1"* ]]; then
    echo -e "DNS 服务器指向       : \${GREEN}✅ 国际标准 (1.1.1.1)\${NC}"
else
    echo -e "DNS 服务器指向       : \${RED}❌ 异常 (含内网IP?)\${NC}"
fi

# 2.3 NSCD 缓存检测
if systemctl is-active --quiet nscd; then
    echo -e "本地 DNS 内存缓存    : \${GREEN}✅ NSCD 运行中 (0ms 解析)\${NC}"
else
    echo -e "本地 DNS 内存缓存    : \${RED}❌ 未运行 (解析起步慢)\${NC}"
fi

# ===========================
# 3. 防火墙与流控 (The Gatekeeper)
# ===========================
echo -e "\n\${YELLOW}[3. 防火墙与流控]\${NC}"

# 3.1 MSS 钳制 (配合手机1400)
MSS_RULE=\$(iptables -t mangle -L OUTPUT -n | grep "TCPMSS set 1360")
if [[ -n "\$MSS_RULE" ]]; then
    echo -e "MSS 强制钳制 (1360)  : \${GREEN}✅ 已生效 (防网页转圈/防卡顿)\${NC}"
else
    echo -e "MSS 强制钳制 (1360)  : \${RED}❌ 规则缺失 (严重隐患)\${NC}"
fi

# 3.2 QUIC 阻断
QUIC_RULE=\$(iptables -L OUTPUT -n | grep "udp dpt:443")
if [[ -n "\$QUIC_RULE" ]]; then
    echo -e "QUIC (UDP) 物理阻断  : \${GREEN}✅ 已封杀 (逼迫浏览器走TCP高速)\${NC}"
else
    echo -e "QUIC (UDP) 物理阻断  : \${RED}❌ 未阻断 (YouTube可能慢)\${NC}"
fi

# ===========================
# 4. 自动化与运维 (The Autopilot)
# ===========================
echo -e "\n\${YELLOW}[4. 自动化与运维保障]\${NC}"

# 4.1 时间同步
if systemctl is-active --quiet chronyd || systemctl is-active --quiet chrony; then
    echo -e "时间同步守护进程     : \${GREEN}✅ Chrony 运行中 (防协议断连)\${NC}"
else
    echo -e "时间同步守护进程     : \${RED}❌ 未运行\${NC}"
fi

# 4.2 预热脚本
if crontab -l 2>/dev/null | grep -q "warmup_pro.sh"; then
    echo -e "全链路自动预热       : \${GREEN}✅ 已部署 (每分钟保持连接热度)\${NC}"
else
    echo -e "全链路自动预热       : \${RED}❌ 未部署\${NC}"
fi

# 4.3 进程提权
if crontab -l 2>/dev/null | grep -q "boost_proxy.sh"; then
    echo -e "进程 VIP 提权        : \${GREEN}✅ 已部署 (节点进程抢占CPU)\${NC}"
else
    echo -e "进程 VIP 提权        : \${RED}❌ 未部署\${NC}"
fi

# 4.4 日志清理
if [[ -f /etc/cron.daily/cleanup_logs ]]; then
    echo -e "日志自动清理         : \${GREEN}✅ 已部署 (防硬盘爆炸)\${NC}"
else
    echo -e "日志自动清理         : \${RED}❌ 未部署\${NC}"
fi

# ===========================
# 5. 最终连通性测试
# ===========================
echo -e "\n\${YELLOW}[5. 核心业务连通性]\${NC}"
# 测试 Google 解析与连接
Start=\$(date +%s%N)
curl -o /dev/null -s --connect-timeout 2 https://www.google.com
if [ \$? -eq 0 ]; then
    End=\$(date +%s%N)
    Duration=\$(( (End - Start) / 1000000 ))
    echo -e "Google 连接测试      : \${GREEN}✅ 通畅 (耗时: \${Duration}ms)\${NC}"
else
    echo -e "Google 连接测试      : \${RED}❌ 失败 (网络不通?)\${NC}"
fi

echo -e "\n\${CYAN}============================================================\${NC}"
EOF
)



