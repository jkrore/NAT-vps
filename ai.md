Codex 高频短句提示词最终版
一、最重要 10 句
这 10 句覆盖你 80% 的场景。
1. Git 初始化项目
把项目初始化成一个 Git 工程，注意排除不需要的文件。
2. 我不会 Git，让 Codex 代管
我不会 Git，你帮我安全处理分支、提交和 PR，不要直接覆盖 main。
3. 生成 .gitignore
根据当前项目生成合适的 .gitignore，不要改其他文件。
4. 检查不该提交的文件
检查项目里哪些文件不应该提交到 Git，并帮我加入 .gitignore。
5. 首次整理并提交
整理当前项目并做第一次 Git 提交，注意不要提交敏感文件和垃圾文件。
6. 修复报错
根据这个报错修复问题，只做最小修改。
7. 先定位问题
先帮我定位这个问题的根因，不要急着改代码。
8. 加功能
给项目添加这个功能，保持现有功能不变。
9. 生成 README 和 AGENTS
根据真实项目生成 README.md 和 AGENTS.md，不要编造功能。
10. 浏览器自动操作
用浏览器帮我完成这个操作，需要登录、验证码、密钥、付款、删除时停下让我自己处理。

二、Git / GitHub 高频短句
1. 初始化类
把项目初始化成一个 Git 工程，注意排除不需要的文件。
把项目初始化成 Git 工程，生成 .gitignore 和 README.md，排除不该提交的文件。
根据当前项目生成合适的 .gitignore，不要改其他文件。
检查项目里哪些文件不应该提交到 Git，并帮我加入 .gitignore。
整理当前项目并做第一次 Git 提交，注意不要提交敏感文件和垃圾文件。

2. 分支类
新建一个功能分支完成这个需求，不要直接改 main。
在新分支上修改，改完后帮我提交。
不要直接改 main，先创建新分支。
把当前分支安全合并回主分支，有冲突先告诉我。
把这个功能分支合并到 main，有冲突不要强行覆盖。

3. 提交类
检查当前改动，排除不该提交的文件，然后帮我提交。
根据当前改动生成一个清晰的 commit message。
帮我提交当前改动，提交信息用中文。
先检查 git diff，再告诉我这些改动能不能提交。
提交前帮我检查有没有敏感信息、缓存、日志和依赖目录。

4. PR 类
把这次改动整理成一个 PR，标题和描述都帮我写好。
改完后创建 PR，不要直接合并。
帮我写这个 PR 的标题和描述。
帮我审查这个 PR，重点看 bug、安全风险和不该提交的文件。
这个 PR 能不能合并？帮我检查风险。

5. 不会 Git 专用
我不会 Git，你帮我安全处理分支、提交和 PR，不要直接覆盖 main。
我不会 Git 命令，你直接帮我用安全方式完成。
不要让我手动执行 Git 命令，你帮我处理好。
所有修改放到新分支，最后开 PR 给我确认。
不要强推，不要覆盖 main，不要删除分支。

三、README / AGENTS.md / 文档类
1. README
根据项目真实代码生成 README.md，不要编造不存在的功能。
根据当前项目更新 README.md，写清楚安装、运行和配置方法。
帮我把 README.md 写得新手也能看懂。
检查 README.md 有没有过时内容，并更新。
根据 package.json、配置文件和源码更新 README.md。

2. AGENTS.md
根据当前项目生成 AGENTS.md，让以后 AI 改代码时知道项目规则。
生成一个简单的 AGENTS.md，要求 AI 少改文件、先看项目、改完测试。
更新 AGENTS.md，加入项目运行方式、测试方式和不要乱改文件的规则。
把这次修复经验写进 AGENTS.md，避免以后重复犯错。
帮我写一个适合 Codex 使用的 AGENTS.md。

3. docs 文档
生成一份新手能看懂的使用说明，放到 docs 目录。
生成开发者文档，说明项目结构、运行方式和主要模块。
把这些 Markdown 文档整理清楚，目录、标题、格式统一。
根据当前功能生成一份使用教程。
帮我把项目说明文档补全，不要改代码。

四、修 bug 高频短句
1. 报错修复
根据这个报错修复问题，只做最小修改。
修复这个报错，改完后测试一遍。
帮我分析这个报错原因，并修复。
先定位根因，再做最小修改。
不要乱改，只修这个错误。

2. 先分析，不改代码
先帮我定位这个问题的根因，不要急着改代码。
只分析问题，不要修改文件。
帮我找出这个 bug 可能在哪些文件里。
先告诉我修复方案，暂时不要动代码。
帮我复现这个问题，并告诉我原因。

3. 修完说明
修完后告诉我原因、改了哪些文件、怎么验证。
改完后运行测试或启动检查。
如果测试跑不了，告诉我原因。
修复后检查有没有影响现有功能。
把修复过程和结果写清楚。

五、添加功能 高频短句
1. 普通加功能
给项目添加这个功能，保持现有功能不变。
按最小改动实现这个功能，不要大规模重构。
先看项目结构，再实现这个功能。
实现这个功能，风格保持和现有代码一致。
新增功能时不要影响旧功能。

2. 先方案后执行
先看项目结构，给我一个实现方案，暂时不要改代码。
这个需求比较复杂，先给我计划，不要直接动代码。
先告诉我需要改哪些文件，再开始。
先给我最简单可行方案。
不要过度设计，给我最实用的实现。

3. 加配置 / 日志 / 开关
给这个功能加一个配置项，默认不影响现有行为。
给关键流程加必要日志，方便排查问题，不要输出敏感信息。
给这个功能加开关，默认关闭。
把这个参数做成可配置项。
增加错误提示，让用户知道哪里出问题。

六、项目体检 / 清理 高频短句
1. 项目体检
帮我体检这个项目，找出结构、依赖、安全和文档问题，不要修改。
检查这个项目有没有明显问题，只分析不改。
帮我找出项目里的高风险问题。
检查项目结构是否混乱，给我优化建议。
帮我做一次代码质量检查，不要修改文件。

2. 清理垃圾
帮我清理项目里的临时文件、缓存和构建产物，不要删源码。
检查哪些文件可以安全删除，先列出来不要直接删。
帮我找出重复文件和无用文件。
清理项目垃圾文件，但不要删除配置、源码和文档。
帮我把不该提交的垃圾文件加入 .gitignore。

3. 敏感信息检查
检查项目里有没有 token、密码、密钥、cookie，不要打印完整内容。
检查有没有敏感信息被提交风险。
帮我确认 .env、密钥和日志有没有被 Git 跟踪。
找出可能泄露隐私或账号信息的文件。
检查提交前有没有安全风险。

七、运行 / 部署 / 一键脚本
1. 运行项目
帮我运行这个项目，缺什么依赖就告诉我或自动补齐。
帮我启动项目，遇到报错就修复。
根据项目文件判断怎么运行，并告诉我命令。
检查这个项目为什么跑不起来。
帮我把项目跑通。

2. 一键脚本
给项目生成一键启动脚本，Windows 和 Linux 都尽量支持。
帮我生成 Windows 一键运行脚本。
帮我生成 Linux/macOS 一键运行脚本。
脚本出错时不要闪退，要显示错误信息。
把安装依赖和启动项目做成一键脚本。

3. Docker / 部署
根据项目生成 Dockerfile 和 .dockerignore，注意不要打包敏感文件。
帮我修复部署失败的问题，找到原因并给出可运行方案。
帮我把项目部署到这个平台，需要账号或密钥时停下让我自己操作。
检查这个项目适合怎么部署。
生成部署说明文档。

八、浏览器自动操作 / 自动登录
1. 最常用
用浏览器帮我完成这个操作，需要登录、验证码、密钥、付款、删除时停下让我自己处理。
打开这个网站，需要登录时停下，我自己登录，登录后你继续操作。
帮我在网页上操作，不要处理密码、验证码和付款。
遇到敏感操作先停下问我。
不要保存、复制、发送我的密码、cookie、token 和密钥。

2. GitHub 网页
打开 GitHub 帮我操作这个仓库，需要登录时我自己登录，不要删除或合并任何东西。
帮我检查这个 GitHub 仓库状态，不要修改。
帮我创建 GitHub 仓库，默认设为 private。
帮我查看 PR 状态，不要合并。
帮我检查仓库设置，有风险的操作先问我。

3. 部署平台网页
用浏览器帮我部署这个项目，需要登录和填写密钥时停下让我自己操作。
帮我配置这个平台，涉及密钥和付款时停下。
帮我检查部署页面哪里配置错了。
帮我看部署日志，找到失败原因。
帮我完成部署流程，但发布前先让我确认。

九、代码审查 / 对比 / 重构
1. 代码审查
帮我审查这段代码，重点看 bug、安全和性能问题。
检查这次改动有没有破坏现有功能。
帮我找出最可能出问题的地方。
帮我检查有没有重复代码和无用逻辑。
只给建议，不要改代码。

2. 对比分析
对比这两个版本，告诉我差异和风险。
帮我比较这两个方案，选简单稳定的。
帮我看哪个实现更好，重点看稳定性和维护成本。
把这些功能做横向对比表。
找出重复功能和冲突功能。

3. 重构
帮我小范围重构这部分代码，不要改变功能。
先给重构方案，不要直接改。
只重构这个模块，不要动其他地方。
把重复代码提取出来，保持行为不变。
重构后跑测试，确认功能没变。

十、测试 / 验证
帮我给这个功能补充基本测试。
运行现有测试，看看有没有失败。
修复失败的测试，只做必要修改。
帮我验证这个功能是否正常。
没有测试的话，至少做一次启动检查。
告诉我怎么手动测试这个功能。
帮我生成一个简单的测试用例。
检查这次修改有没有边界情况没处理。

十一、最适合你记住的三板斧
1. 项目/Git 三板斧
把项目初始化成一个 Git 工程，注意排除不需要的文件。
我不会 Git，你帮我安全处理分支、提交和 PR，不要直接覆盖 main。
根据真实项目生成 README.md 和 AGENTS.md，不要编造功能。

2. 修 bug 三板斧
先帮我定位这个问题的根因，不要急着改代码。
根据这个报错修复问题，只做最小修改。
修完后告诉我原因、改了哪些文件、怎么验证。

3. 浏览器自动操作三板斧
打开这个网站，需要登录时停下，我自己登录，登录后你继续操作。
用浏览器帮我完成这个操作，需要登录、验证码、密钥、付款、删除时停下让我自己处理。
遇到敏感操作先停下问我，不要保存我的密码、cookie、token 和密钥。

十二、最终万能短句
你懒得想的时候，就用这几句。
Git / 项目万能句
我不会 Git，你帮我用安全方式完成这个需求，注意排除不该提交的文件，改完创建 PR，不要直接合并 main。
修 bug 万能句
根据这个问题定位根因并修复，只做最小修改，改完测试并告诉我改了什么。
加功能万能句
给项目添加这个功能，保持现有功能不变，按最小改动实现。
文档万能句
根据真实项目生成 README.md 和 AGENTS.md，不要编造功能。
浏览器万能句
用浏览器帮我完成这个操作，需要登录、验证码、密钥、付款、删除时停下让我自己处理。

十三、你的使用原则
你以后就按这个格式说：
目标：干什么。边界：不要干什么。交付：最后给我什么。
例如：
把项目初始化成 Git 工程，注意排除不需要的文件，最后告诉我改了什么。
修复这个报错，只做最小修改，改完测试一下。
用浏览器帮我部署项目，需要登录和密钥时停下让我自己操作。
这就是最适合 Codex 的方式：
短、清楚、有边界。
